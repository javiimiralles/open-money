import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function TransactionsScreen() {
  return (
    <PlaceholderScreen
      title="Movimientos"
      description="Aquí verás tus ingresos, gastos y transferencias, con filtros por categoría, fechas y cuenta."
    />
  );
}